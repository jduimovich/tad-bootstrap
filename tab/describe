SCRIPTDIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )" 
source $SCRIPTDIR/configure  
 
echo "Template Repository: $TEMPLATE_REPO"
echo "Template Directory: $TEMPLATE_DIR" 

echo "Tekton in: $TEKTON" 