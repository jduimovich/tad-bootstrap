
# use readlink as the way to install is to install is via softlink
# ln -s PATH-TO-/tad-bootstrap/tab/tab  path-to-your-bin/tab 
PHYSICAL_FILE=$(realpath $(readlink $0)) 
SCRIPTDIR=$(dirname $PHYSICAL_FILE) 
source $SCRIPTDIR/../common/cli 
  