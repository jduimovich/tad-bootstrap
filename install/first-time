SCRIPTDIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )" 
ROOT=$(realpath $SCRIPTDIR/..) 

REPONAME=$(git config --get remote.origin.url) 
APPNAME=$(basename $REPONAME .git)
APP_TYPE=http 
 
echo "localizing this template to the git repo location and name" 
bash $ROOT/install/$APP_TYPE  $APPNAME $APPNAME-ns  quay.io/jduimovich0/single-nodejs-app 
tree components 
git add .
git commit -m "repo localized to repo and names"
git push 