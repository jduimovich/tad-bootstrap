SCRIPTDIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )" 
ROOT=$(realpath $SCRIPTDIR/..) 

bash $ROOT/install/service  test-service my-namespace  quay.io/jduimovich0/single-nodejs-app 
tree components/ 